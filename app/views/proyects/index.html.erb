<!DOCTYPE html>
<html>
<head>
	<title></title>
	<style>
	body {
		background-color: pink;
	}

	h3{
		color: blue;
	}

	li {
		background-color: green;
		color: white;
		margin-top: 50px;
	}
	</style>
</head>
<body>
<h3>List</h3>
<ul><% @projects.each do |project|%>
	<li>
		<%= project.name %>
		<% if project.description != nil %>
			<%= ' - ' + project.description %>
			<% end %>
		<% end %>
	</li>
</ul>
</body>
</html>